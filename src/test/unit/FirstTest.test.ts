describe('first unit test', () => {
    describe('sum', () => {
        it('should return 2 with 1 plus 1', () => {
            let expected = 2;
            let actual = 1 + 1;

            expect(actual).toEqual(expected);
        })
    })
})